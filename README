Steps to fun...

1. Install Vundle

git clone http://github.com/gmarik/vundle.git ~/.vim/bundle

2. Run vim with BundleInstall

vim -c "execute \"BundleInstall\" | q | q"

You can make this easier by getting oh-my-zsh and then just doing...

➜  ~  vundle

